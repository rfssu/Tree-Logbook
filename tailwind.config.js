/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./web/**/*.{html,js}"],
    theme: {
        extend: {
            colors: {
                // We can add custom JokoUI colors here later
            }
        },
    },
    plugins: [],
}
